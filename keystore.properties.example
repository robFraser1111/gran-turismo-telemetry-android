# Copy to keystore.properties (gitignored) and fill in. Never commit the real file or the .jks.
# Paths are relative to the repo root.
storeFile=upload-keystore.jks
storePassword=REPLACE_ME
keyAlias=upload
keyPassword=REPLACE_ME
